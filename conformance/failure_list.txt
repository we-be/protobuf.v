# Known conformance failures for protobuf.v (proto3): NONE.
#
# Every applicable Required.Proto3 case passes:
#   1468 successes, 4134 skipped, 0 expected failures, 0 unexpected failures.
# Skipped = proto2, editions, text-format, and JSPB — all documented non-goals.
#
# A handful of *Recommended* (optional, non-conformance-gating) cases are not
# yet handled and surface only as warnings, not failures:
#   - Value holding NaN/Infinity should fail JSON serialization (we emit the
#     string spelling).
#   - JSON_IGNORE_UNKNOWN parsing of unknown enum *string* names (we error).
#   - FieldMask paths that don't round-trip should fail serialization.
# These are candidates for a future release; none affect the Required suite.
